<%= form_for @attendance do |f|%>
	<% if @attendance.new_record? %>
		<%= f.submit 'Clock In', class: "button success tiny" %>
	<% else %> 
		<%= f.submit 'Clock Out', class: "button alert tiny" %>
	<% end %>
<% end %>

