<% provide(:title, "Attendance") %>
<br>
<div align="center">
	<%= render 'form' %>
	<%= render 'attendance'%>
	<%= link_to "Request Leave", new_leave_path, class: "button success tiny" if current_user.employee?  %>
	<%= link_to "Request Access", new_access_path, class: "button warning tiny" if current_user.employee? %>
	<%= link_to "Request Equipment", new_equipment_path, class: "button notice tiny" if current_user.employee? %>
</div>
